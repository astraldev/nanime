---
title: Loading sequence
description: A build log that runs, pauses, and resumes, scrambling text between steps.
navigation:
  icon: i-ph-spinner-gap
seo:
  title: "Loading sequence example: scramble text in Nuxt"
  description: A build log that runs, pauses and resumes, scrambling text between steps with useScrambleText and useWaapiAnimate in Nuxt. Live demo and build guide.
---

A terminal-style build log you can run, pause and resume. Pausing stops the
spinner and the step timer together. On resume, the spinner carries on from
the angle it stopped at.

::render-code-block-preview
---
code: false
src: examples/showcase/loading-sequence/LoadingSequenceDemo.vue
---
::

## What's happening

Run starts the log ticking through build steps one at a time, with a
spinner turning next to the status line. Each step's text scrambles through
random characters before settling on the new line. When the last step
finishes, a checkmark pops in and replaces the spinner.

## How it's built

Three composables split the work by state. The icon's spin runs off the
main thread through [`useWaapiAnimate`](/composables/use-waapi-animate), so
it does not stutter under other work. The text between steps scrambles
through [`useScrambleText`](/composables/use-scramble-text). The checkmark
pop-in at the end uses [`useAnimate`](/composables/use-animate).

Each step waits out its scramble duration plus a custom step delay before
advancing. Pausing stops that wait. Resuming restarts the current step's full
wait, not the part left over from before the pause.

The scramble needs no imperative trigger. Each step writes its text to the
target `useScrambleText` watches, so a new scramble starts the moment the step
advances.

The spinner and the checkmark are each scoped to their state: the spinner
shows only while loading, the checkmark only on success. Each composable
attaches once its element appears, so neither has to exist before its state
does.

## Building it yourself

1. Define the steps as a plain array, each with its status text and an optional step delay.
2. Track state as `'idle' | 'loading' | 'success'`, plus the current step index and status text.
3. Render the spinner only while `state === 'loading'`, and turn it with [`useWaapiAnimate`](/composables/use-waapi-animate) on a linear, looping rotation to 360°.
4. Bind the status line to [`useScrambleText`](/composables/use-scramble-text), with the target text in a `ref` that each step writes to. Changing that text is enough to trigger a new scramble.
5. Advance with a `setTimeout` chain: scramble duration plus a step delay.
6. Pause by clearing the timer and pausing the spinner instance. Resume by playing it again and setting a fresh timer for the current step's full wait.
7. On the last step, switch to `'success'` and animate a checkmark in with [`useAnimate`](/composables/use-animate).

::collapsible{name="AI build prompt"}
```text
Using the nanime Nuxt module, build a Vue 3 terminal-style build-log
component with three states: idle, loading, success. Define an array of
build step objects, each with its status text and an optional custom delay
that mimics the time it takes to complete that work.

On start, cycle through the steps with a setTimeout chain, showing
"[n/total] step text" as the current status. Bind the status text to
useScrambleText(statusEl, {}, { text: currentText, chars:
'0123456789abcdef', settleDuration: 300, revealRate: 50 }) using a reactive
computed target, so each step change triggers a new scramble automatically
without any imperative call. Calculate step duration by multiplying
(text length minus 1) by 20 and adding 300.

While state is loading, render a spinner icon animated with
useWaapiAnimate(spinnerEl, { rotate: { to: 360 }, duration: 800,
ease: 'linear', loop: true }).

Add pause and resume: pausing calls clearTimeout on the step timer and
pauses the spinner instance. Resuming plays the spinner again and sets a new
timeout for the current step's full wait (its computed scramble duration
plus the custom step delay), not the portion that was left when it paused.

When the last step completes, switch state to success, show a checkmark
element, and animate it in with useAnimate (opacity from 0 to 1, scale
from 0.8 to 1, outQuad ease, 300ms).
```
::
