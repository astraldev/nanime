---
title: useWaapiAnimate
description: Documentation for useWaapiAnimate composable.
navigation:
  icon: i-ph-lightning
---

::nuxt-link{to="/composables/introduction#instant-play"}
  :::badge
  ---
  icon: mage:zap-fill
  label: Instant play
  size: md
  variant: soft
  ---
  :::
::

The `useWaapiAnimate` composable wraps the [waapi.animate](https://animejs.com/documentation/web-animation-api) function
from AnimeJS to help create [WAAPI](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API) powered animations.

## Arguments

::field-group
  :::field{required name="target" type="WaapiTargets"}
  The DOM element(s) to animate.
  :::

  :::field{name="parameters" type="MaybeRefOrGetter<WAAPIAnimationParams>"}
  Object containing keyframes and animation options.
  :::
::

## Example

:render-code-block-preview{src="examples/composables/WaapiAnimateDemo.vue"}

## Return Value

Returns a `shallowReactive` object wrapping the `WAAPIAnimation` instance.

## API

### Types

```ts
function useWaapiAnimate(
  target: WaapiTargets,
  parameters?: MaybeRefOrGetter<WAAPIAnimationParams>
): WAAPIAnimation
```

::collapsible{name="Type aliases"}
```ts
type WaapiElementTargets = VueInstance | HTMLElement | HTMLElement[] | SVGElement | SVGElement[] | NodeList | string | null
type WaapiTargets = MaybeRef<WaapiElementTargets> | MaybeRef<WaapiElementTargets>[]
```
::

See [AnimeJS WAAPI documentation](https://animejs.com/documentation/web-animation-api) for more details.

## Caveats

- Plain JS objects are not valid targets. The Web Animations API only animates elements, so use [`useAnimate`](/composables/use-animate) for objects and numbers.
- Don't destructure the return value. The instance is wrapped in `shallowReactive`, and destructuring copies its values out once, disconnecting them from the instance.

```ts
// loses the connection to the instance
const { play } = useWaapiAnimate('.target', options)

// keeps it
const animation = useWaapiAnimate('.target', options)
animation.play()
```

::tip
Call `useWaapiAnimate` synchronously in `setup()` for it to react to `target` and `parameters`
changes. Called from `onMounted`, a watcher, or a handler, it runs in
[instant mode](/composables/introduction#instant-play): the animation is
created once and `parameters` are read only that first time.
::
