---
title: Introduction
description: Every nanime composable, and how they pick reactive or instant mode.
navigation:
  icon: i-ph-squares-four
seo:
  title: 'Composables overview: reactive and instant modes'
  description: 'All eight nanime composables, the Anime.js API each one wraps, and when each runs in reactive, instant or buffered mode depending on where it is called.'
---

`nanime` provides one composable for each
[AnimeJS](https://animejs.com/){target="_blank"} API. Every page includes an
example.

::u-page-grid
  :::u-page-card
  ---
  icon: i-ph-play-circle
  title: useAnimate
  to: /composables/use-animate
  variant: soft
  ---
  #description
  Reactive wrapper for the [animate]{.text-primary} function
  :::

  :::u-page-card
  ---
  icon: i-ph-lightning
  title: useWaapiAnimate
  to: /composables/use-waapi-animate
  variant: soft
  ---
  #description
  Reactive wrapper for the [waapi.animate]{.text-primary} function
  :::

  :::u-page-card
  ---
  icon: i-ph-cube
  title: useAnimatable
  to: /composables/use-animatable
  variant: soft
  ---
  #description
  Wrapper for the [createAnimatable]{.text-primary} function
  :::

  :::u-page-card
  ---
  icon: i-ph-text-t
  title: useSplitText
  to: /composables/use-split-text
  variant: soft
  ---
  #description
  Wrapper for the [splitText]{.text-primary} utility
  :::

  :::u-page-card
  ---
  icon: i-ph-shuffle
  title: useScrambleText
  to: /composables/use-scramble-text
  variant: soft
  ---
  #description
  Reactive wrapper for the [scrambleText]{.text-primary} utility
  :::

  :::u-page-card
  ---
  icon: i-ph-list-numbers
  title: useAnimeTimeline
  to: /composables/use-anime-timeline
  variant: soft
  ---
  #description
  Reactive wrapper for the [createTimeline]{.text-primary} function
  :::

  :::u-page-card
  ---
  icon: i-ph-hand-grabbing
  title: useDraggable
  to: /composables/use-draggable
  variant: soft
  ---
  #description
  Wrapper for the [createDraggable]{.text-primary} function
  :::

  :::u-page-card
  ---
  icon: i-ph-mouse-scroll
  title: useAnimeScroll
  to: /composables/use-anime-scroll
  variant: soft
  ---
  #description
  Reactive wrapper for the [onScroll]{.text-primary} function
  :::
::

## Client-side lifecycle and reactivity

Every `nanime` composable runs client-only: animations require the DOM and render
nothing during server-side rendering (SSR).

When used inside `setup()`, every composable watches its inputs and automatically
rebuilds when a reactive argument (a `ref`, getter, or reactive object) changes.
The only exception is [`useAnimatable`](/composables/use-animatable), which
rebuilds only when its `target` changes. Mutations to its `options` do not
rebuild the animatable controller.

## Instant play

Composables marked with the
:badge{icon="mage:zap-fill" label="Instant play" size="md" variant="soft"}
work outside `setup()`. You can call them after mount from an event handler or a
utility function. Their mode depends on whether
[`getCurrentInstance()`](https://vuejs.org/api/composition-api-helpers.html#getcurrentinstance)
returns an active instance when they run:

- **Reactive mode** runs when an instance exists. `nanime` watches targets and
  parameters. Changing a `ref`{lang="ts-type"} or a getter rebuilds the
  animation. With the default `keepTime: false`, the animation reverts on each
  change. Setting `keepTime: true` carries the playhead across the rebuild
  instead of restarting.
- **Instant mode** runs when no instance is present. `nanime` creates the
  animation once. It reads `parameters` a single time, even if you pass a
  `ref`{lang="ts-type"}.

A top-level `await` in `<script setup>` stays in reactive mode. The Vue compiler
wraps the call in
[`withAsyncContext()`](https://vuejs.org/api/sfc-script-setup.html#top-level-await),
which restores the instance after the promise settles. Other asynchronous
contexts run without an instance. These include event handlers, watchers,
`setTimeout` callbacks, nested async functions, and Options API `async setup()`
hooks.

```ts
// instant: this runs after setup(), so there is no instance to find
button.addEventListener('click', () => {
  const anim = useAnimate('.box', () => ({ opacity: opacityRef.value }))
  // opacityRef changes are ignored from here on
})
```

::tip
`nanime` does not warn you when this happens, and the animation still runs.
Only reactivity is lost. To keep reactivity and trigger playback later, call
the composable in `setup()` and control it through the returned instance.
::

`useDraggable`, `useSplitText`, and `useAnimeScroll` do not use this mechanism.
They have no instant mode and must be called from `setup()`, because they depend
on the component's mount lifecycle.

## Buffered

Composables marked with the
:badge{icon="ph:stack" label="Buffered" size="md" variant="soft"}
return a buffered proxy instead of a plain reactive one. A chainable method
is one that returns the instance, like `play()`. Call one before the
underlying instance exists, and `nanime` queues it, then replays every
queued call, in order, once the instance is created. A property, or a
method that returns something other than the instance, reads as
`undefined` until then.

[`useDraggable`](/composables/use-draggable),
[`useAnimeTimeline`](/composables/use-anime-timeline), and
[`useAnimeScroll`](/composables/use-anime-scroll) return buffered proxies.
Their instances are created after mount, or immediately for
`useAnimeTimeline` in [Instant mode](#instant-play).
