---
title: Configuration
description: Configuring nanime options for your Nuxt project.
navigation:
  icon: i-lucide-settings
seo:
  title: Configuration - nanime
  description: Learn how to configure nanime composables.
---

`nanime` is designed to work with zero configuration, but you can customize it to fit your needs.

## Module Options

You can pass options to the module directly in `nuxt.config.ts`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['nanime'],
  nanime: {
    // Options
  }
})
```

### `composables`
- **Type**: `boolean`
- **Default**: `true`

Whether to auto-import composables (`useAnimate`, `useWaapiAnimate`).

```ts
nanime: {
  composables: false // Disable auto-imports
}
```
