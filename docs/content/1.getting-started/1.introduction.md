---
title: Introduction
description: SSR-safe animations with AnimeJS.
navigation:
  icon: i-lucide-info
seo:
  title: 'Introduction: Anime.js composables for Nuxt'
  description: 'What nanime adds to Anime.js v4 in Nuxt: SSR-safe composables, reactive targets, auto-imports and cleanup on unmount, with links to each composable.'
---

`nanime` is a Nuxt module that brings
[AnimeJS](https://animejs.com/) into Vue and Nuxt as composables you can
call straight from `setup()`.

## Why?

AnimeJS needs the DOM. In Nuxt, that means an `onMounted` guard, cleanup on
unmount, and preventing hydration mismatches. You write that same handful of
lines in every component that animates anything. `nanime` keeps them in the
composables instead.

## What you get

- **SSR safe**: Nothing runs on the server, so you do not need `onMounted`
  wrappers or `<ClientOnly>` checks.
- **Reactive**: Targets accept a `ref`, a `computed` or a getter. The
  animation rebuilds when targets change.
- **Auto-imported**: The module registers them, so there is nothing to import
  and no plugin to set up.

## The composables

| Composable                                             | Wraps                 | What it does                                               |
| ------------------------------------------------------ | --------------------- | ---------------------------------------------------------- |
| [`useAnimate`](/composables/use-animate)               | `animate()`           | CSS properties, transforms, SVG and plain JS objects       |
| [`useWaapiAnimate`](/composables/use-waapi-animate)    | `waapi.animate()`     | Web Animations API, off the main thread                    |
| [`useAnimatable`](/composables/use-animatable)         | `createAnimatable()`  | Property setters that tween, for values that change a lot  |
| [`useAnimeTimeline`](/composables/use-anime-timeline)  | `createTimeline()`    | Several animations on one clock                            |
| [`useSplitText`](/composables/use-split-text)          | `splitText()`         | Text split into lines, words and characters                |
| [`useScrambleText`](/composables/use-scramble-text)    | `scrambleText()`      | Scramble and reveal effects, with reactive text            |
| [`useDraggable`](/composables/use-draggable)           | `createDraggable()`   | Dragging, with snap, bounds and axis constraints           |
| [`useAnimeScroll`](/composables/use-anime-scroll)      | `onScroll()`          | Drive animations from scroll position                      |
| [`useAnimeLayout`](/composables/use-anime-layout)      | `createLayout()`      | Animate position and size when the layout changes          |

## The components

[`<AnimeTransition>`](/components/transitions) and
[`<AnimeTransitionGroup>`](/components/transitions#lists) work like
Vue's `<Transition>` and `<TransitionGroup>`. AnimeJS runs the enter, leave
and move animations, so there is no CSS to write. Both take the name of a
[transition style](/components/transition-styles), or AnimeJS params.

## Using nanime with AI agents

`nanime` ships its own agent skill and machine-readable docs:

- **Agent skill**: `npx skills add astraldev/nanime --skill nanime`
- **llms.txt**: [/llms.txt](https://nanimejs.netlify.app/llms.txt){target="_blank"} and [/llms-full.txt](https://nanimejs.netlify.app/llms-full.txt){target="_blank"}
- **MCP server**: `https://nanimejs.netlify.app/mcp`

[Use with AI agents](/getting-started/installation#use-with-ai-agents)
covers what each one holds and how to add it.

## Next steps

- [Install the Nuxt module](/getting-started/installation) to add the package.
- [Composables overview and reactive modes](/composables/introduction) for each API, with a live demo on every page.
- [Transition components](/components/transitions) for animating elements and lists in and out.
- [Showcase examples](/examples/introduction) for complete motion sequences built from several composables.
- [Animation performance](/getting-started/performance) for which engine to pick, and what patterns keep animations fast.
- [vs Nuxt modules](/getting-started/vs-nuxt-modules) and
  [vs Vue packages](/getting-started/vs-vue-packages) for how `nanime`
  differs from AnimeJS on its own and from other wrappers.
