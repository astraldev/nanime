---
title: Proxies
description: Re-exported anime.js helpers for use inside composable parameters.
navigation:
  icon: i-ph-plugs-connected
---

> Re-exported anime.js helpers for use inside composable parameters without importing directly from `animejs/*`.

## Text

Available via `#nanime/proxies/text`.

| Export | Description |
| --- | --- |
| `scrambleText` | Creates a scramble/reveal text effect for use as an `innerHTML` animation value |
| `ScrambleTextParams` | Type for scrambleText options |

```ts
import { scrambleText } from '#nanime/proxies/text'

const tl = useAnimeTimeline({ loop: true })

tl.add(target, {
  innerHTML: scrambleText({
    text: 'hello world',
    chars: 'symbols',
  }),
})
```

See the [useScrambleText composable](/composables/use-scramble-text) for the reactive wrapper, or the [AnimeJS Scramble Text documentation](https://animejs.com/documentation/text/scrambletext){target="_blank"} for all available options.
